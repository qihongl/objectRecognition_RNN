rexp(10,1)
rexp(10,.5)
rexp(10,.5)
rexp(10,.5)
rexp(10,.5)
rexp(10,.2)
rexp(10,.2)
rexp(10,.2)
rexp(10,.2)
help xyplot
? xyplot
rnorm(100)
rnorm(100)
rnorm(20)
library(lattice)
xyplot(rnorm(20))
? xyplot
rm(list = ls())
library(ggplot2); library(ggdendro); library(ape)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
rm(list = ls())
library(ggplot2); library(ggdendro); library(ape)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
datapath
temp = read.table(datapath)
head(temp)
n = length(temp)
numPatterns = dim(temp)[1]
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
hiddenData
? writeMat
?? writeMat
install.packages(R.matlab)
install.packages('R.matlab')
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
hiddenData
writeMat(hiddenData)
getwd
getwd()
write.table(hiddenData, file = "hiddenData.csv")
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
hiddenData
write.table(hiddenData, file = "hiddenData.csv")
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
hiddenData
RDM = matrix(data=NA,nrow=3,ncol=3)
RDM
numPatterns
numPatterns
hiddenData[1,]
RDM[i][j]
i=1
j=1
RDM[i][j]
RDM[i][j] = cor(hiddenData[i,], hiddenData[j,])
RDM[i][j]
for (i in 1:numPatterns ) {
for (j in 1:numPatterns ) {
RDM[i][j] = cor(hiddenData[i,], hiddenData[j,])
}
}
dim(hiddenData)
RDM
image(RDM)
image(RDM)
RDM
dim(RDM)
RDM
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
# RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1:numPatterns ) {
for (j in 1:numPatterns ) {
RDM[i][j] = cor(hiddenData[i,], hiddenData[j,])
}
}
RDM = matrix(0, 48, 48)
for (i in 1:numPatterns ) {
for (j in 1:numPatterns ) {
RDM[i][j] = cor(hiddenData[i,], hiddenData[j,])
}
}
warning()
RDM
for (i in 1:numPatterns ) {
for (j in 1:numPatterns ) {
RDM[i][j] = cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
RDM
cor(hiddenData[i,], hiddenData[j,])
printf("hello %d\n", 56)
print(i);print(j);
print(i,j)
cat(i,j)
print(cat(i,j))
print(cat(i,j))
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1:numPatterns ) {
for (j in 1:numPatterns ) {
print(cat(i,j))
RDM[i][j] = cor(hiddenData[i,], hiddenData[j,])
}
}
RD,
RDM
RDM[1][1]
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
i = 1
RDM[i,j]
RDM[i,j]
j = 1;
RDM[i,j]
j in 1 : 10
for (j in 1 : 10){
RDM[i,j] = cor(hiddenData[i,], hiddenData[j,])
}
RDM[1][1]
RDM
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
i = 1
for (j in 1 : numPatterns){
RDM[i,j] = cor(hiddenData[i,], hiddenData[j,])
}
RDM
RDM
rm(list = ls())
library(R.matlab)
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1 : numPatterns){
for (j in 1 : numPatterns){
RDM[i,j] = cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
image(RDM)
diag(RDM)
? image
matrix(1:9)
matrix(1:9,3,3,)
matrix(1:9,3,3)
image(matrix(1:9,3,3))
t(matrix(1:9,3,3)
)
image(t(matrix(1:9,3,3)))
t(matrix(1:9,3,3))
? image
image(~,~t(matrix(1:9,3,3)))
image(~,~,t(matrix(1:9,3,3)))
image(1:9,1:9,t(matrix(1:9,3,3)))
? image
image(t(matrix(1:9,3,3)),t(matrix(1:9,3,3)),t(matrix(1:9,3,3)))
image(1:9,1:9,1:9)
image(1:9,1:9,matrix(3,3,3))
image(1:9,1:9,matrix(1:9,3,3))
rm(list = ls())
source('image.scale.R')
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1 : numPatterns){
for (j in 1 : numPatterns){
RDM[i,j] = cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
image.scale(RDM)
image(RDM)
x = matrix(1:9,3,3)
x
image(x)
x
x[1,1]
x[1,1] = 10
image(x)
require(grDevices) # for colours
x <- y <- seq(-4*pi, 4*pi, len = 27)
r <- sqrt(outer(x^2, y^2, "+"))
image(z = z <- cos(r^2)*exp(-r/6), col  = gray((0:32)/32))
image(z, axes = FALSE, main = "Math can be beautiful ...",
xlab = expression(cos(r^2) * e^{-r/6}))
contour(z, add = TRUE, drawlabels = FALSE)
image(RDM)
x
x
x = matrix(1:19)
x = matrix(1:4,2,2)
image(x)
x
x = matrix(c(1, 0, 0, 0),2,2)
image(x)
x
x = matrix(c(1, 1, 0, 0),2,2)
image(x)
x = matrix(c(1, 0, 0, 0),2,2)
image(x)
x
x = matrix(c(0, 1, 0, 0),2,2)
x
image(x)
? image
image(1,1,1)
display(x)
myImagePlot
rm(list = ls())
source('myImagePlot.R')
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1 : numPatterns){
for (j in 1 : numPatterns){
RDM[i,j] = cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
myImagePlot(RDM)
image(RDM)
rm(list = ls())
source('myImagePlot.R')
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1 : numPatterns){
for (j in 1 : numPatterns){
RDM[i,j] = 1-cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
myImagePlot(RDM)
image(RDM)
myImagePlot(RDM)
myImagePlot(RDM)
image(RDM)
myImagePlot(RDM)
range(RDM)
RDM
rm(list = ls())
source('myImagePlot.R')
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
RM =  matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1 : numPatterns){
for (j in 1 : numPatterns){
RM[i,j] = cor(hiddenData[i,], hiddenData[j,])
RDM[i,j] = 1-cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
myImagePlot(RDM)
image(RDM)
RM
summary(RM)
mean(RM)
std(RM)
sd(RM)
summaries(RM)
summary(RM)
range(RM)
rm(list = ls())
source('myImagePlot.R')
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1 : numPatterns){
for (j in 1 : numPatterns){
RDM[i,j] = 1-cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
myImagePlot(RDM)
rm(list = ls())
source('myImagePlot.R')
PROJECT_DIR = '/Users/Qihong/Dropbox/github/categorization_PDP'
setwd(PROJECT_DIR)
# you need to enter the file name and folder name here!
DATA_FOLDER = 'sim16_large'
FILENAME = 'hiddenFinal_e3.txt'
# load the data
datapath = paste(PROJECT_DIR, DATA_FOLDER, FILENAME, sep="/")
temp = read.table(datapath)
# set some parameters
n = length(temp)
numPatterns = dim(temp)[1]
# convert the hidden activation to a matrix
hiddenData = as.matrix(temp[1:numPatterns,4:n])
temp = as.vector(temp[1:numPatterns,2])
temp = sapply(strsplit(temp, split='l', fixed=TRUE), function(x) (x[2]))
row.names(hiddenData) = temp
RDM = matrix(data=NA, nrow=numPatterns, ncol=numPatterns)
for (i in 1 : numPatterns){
for (j in 1 : numPatterns){
RDM[i,j] = 1-cor(hiddenData[i,], hiddenData[j,])
}
}
RDM
myImagePlot(RDM)
